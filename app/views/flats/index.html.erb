<div class="container">
<h1 class="title-offers">All the available offers</h1>
  <div class="grid-container">
    <%@flats.each do |flat|%>
      <div class="card-trip">
        <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
        <div class="card-content">
          <div class="card-trip-infos">
            <div>
              <h2><%= flat.title %></h2>
            </div>
            <h2 class="card-trip-pricing"><%= flat.price %> €</h2>
          </div>
          <div class="button-offer-container">
            <%=link_to "Check offer", flat_path(flat), class: "button-offer" %>
          </div>
        </div>
      </div>
    <%end%>
  </div>
  <%=link_to "Add new flat", new_flat_path, class: "text-right"%>
</div>
