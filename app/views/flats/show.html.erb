<h1>This is the show.</h1>
<br>
<% if @flat.photo.attached? %> 
        <%= cl_image_tag @flat.photo.key, width: 150, height: 150, crop: :thumb, gravity: :face %> 
      <% else %> 
        <%= "You can upload a picture"%> 
<% end %>
<%= @flat.title %>
<%= @flat.city %>
<%= @flat.description %>
<%= @flat.price %>
<%= link_to "Back", flats_path%>
